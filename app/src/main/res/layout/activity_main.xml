<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.rapturedshadows.gunsquad.Main"
    android:background="@drawable/bgmainmenu"
    >

    <Button
        android:text="New Game"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_marginLeft="24dp"
        android:layout_marginStart="24dp"
        android:layout_marginBottom="95dp"
        android:id="@+id/newgame"
        android:layout_width="wrap_content"
        style="@android:style/Widget.Holo.Button.Inset"
        android:textColor="@android:color/background_dark" />

    <Button
        android:text="Settings"
        android:layout_height="50dp"
        android:layout_alignParentBottom="true"
        android:layout_alignRight="@+id/continuegame"
        android:layout_alignEnd="@+id/continuegame"
        android:id="@+id/settings"
        android:background="@drawable/buttonskin"
        android:backgroundTint="@android:color/darker_gray"
        android:layout_width="100dp" />

    <Button
        android:text="Continue"
        android:layout_height="50dp"
        android:id="@+id/continuegame"
        android:layout_above="@+id/settings"
        android:layout_alignRight="@+id/newgame"
        android:layout_alignEnd="@+id/newgame"
        android:background="@drawable/buttonskin"
        android:backgroundTint="@android:color/darker_gray"
        android:layout_width="100dp" />

</RelativeLayout>
